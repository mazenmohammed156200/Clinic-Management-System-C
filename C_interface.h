#include "STD.h"


u8 password();//done

struct Patient*  ID_search(struct Patient* head, u32 id);//done

void View_Patient(struct Patient* head);//done

void Add_Patient(struct Patient** head);//done

void Edit_Patient(struct Patient* head);//done

void reservation_Init(); //done

void reserve_slot(struct Patient* head); //done

void cancel_reserve(struct Patient* head); //done

void View_Reservations();//done

void free_space(struct Patient* head);

////////////////////////////main //done
